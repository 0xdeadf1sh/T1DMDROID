# ── Private / never-ship: working notes, secrets, local config ──────────────
# (see CLAUDE.md private/public seam — these must never enter the GPL tree)
*.private.md
PLAN.private.md
NOTES.md
scratch/
scratchpad/
local.properties
keystore.properties
*.jks
*.keystore
secrets.properties
*.local.*

# ── Android / Gradle ────────────────────────────────────────────────────────
.gradle/
build/
captures/
.cxx/
*.apk
*.aab
*.ap_
*.dex
output.json

# ── Kotlin / IntelliJ / Android Studio ──────────────────────────────────────
.idea/
*.iml
.kotlin/

# ── Rust (t1dm-core) ────────────────────────────────────────────────────────
target/
**/target/
# Cargo.lock is intentionally committed (app, not a library).

# ── Large model binaries (keep out of git history; use LFS / release assets) ─
*.pt
*.pte
models/*.bin
# generated .pte + descriptor.json (regenerate via T1DMAI/exporters)
models/exported/

# ── Claude Code ─────────────────────────────────────────────────────────────
# Per-user Claude state stays local; share the project skills with contributors.
.claude/*
!.claude/skills/

# ── Misc ────────────────────────────────────────────────────────────────────
.DS_Store
*.log

